{
  "mcpServers": {
    "magento-server": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--network=host",
        "-e",
        "MCP_CLIENT_URL=http://127.0.0.1:8090",
        "mcp/magento-server"
      ],
      "env": {}
    }
  }
}